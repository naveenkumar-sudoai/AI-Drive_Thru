# Copy this file to `.env` and fill in your real DeepSeek API key, then the
# systemd voice service (which reads this via EnvironmentFile) will pick it up:
#   cp .env.example .env
#   nano .env
#
# `.env` is gitignored, so your key is never committed.
DEEPSEEK_API_KEY=
